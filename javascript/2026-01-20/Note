indexOf
- return index of element which you need
- -1 -> not exists
- != -1 -> exists
- can be use to check existing element
- if there are many elements
  -> first index element from the index you want to find
  -> not found -> return -1

includes
- array.includes(<element>)
  -> true / false
- use to check existing element
- check substring
  const productName = 'Gao ST25 (5kg)'
  console.log(productName.includes('gao'))

find
- use when you make sure the result only 1 elements
- base on loop (forEach) -> O(n)
- return only 1 element if callback func return true
- return undefined if callback func return false

-> find by name, price > 1000
-> many elements
